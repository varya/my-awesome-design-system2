import { Meta, Canvas, Story, ArgsTable } from "@storybook/addon-docs";

import Carousel from ".";
import cat1 from "./cat1.png";
import cat2 from "./cat2.png";
import cat3 from "./cat3.png";
import cat4 from "./cat4.png";

<Meta title="Components/Carousel" />

## Carousel

An image carousel/slider with swipe support and dot navigation

<!-- We hve to write "export const" only because MDX won't let us simply declare it (https://github.com/mdx-js/mdx/issues/390#issuecomment-461158808) -->

export const images = [
  { source: cat1 },
  { source: cat2 },
  { source: cat3 },
  { source: cat4 },
];

<Canvas>
  {/* Simple story without "controls" tab at the bottom.
   * This tab is enabled by default, we can hide it explicitly (otherwise it will be shown empty */}
  <Story name="Default" parameters={{ controls: { disable: true } }}>
    <Carousel images={images} style={{maxHeight: "300px"}} />
  </Story>
</Canvas>

## Props

<ArgsTable of={Carousel} />
