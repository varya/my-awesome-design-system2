import { Meta, Canvas, Story, ArgsTable } from "@storybook/addon-docs";

import SummaryTable from ".";

<Meta
  title="Components/SummaryTable"
  parameters={{
    viewport: {
      defaultViewport: "mobile1",
    },
  }}
/>

export const tableArgs = {
  items: [
    { name: "Subtotal", price: "€95.00" },
    { name: "Estimated Delivery & Handling", price: "€0.00" },
  ],
  total: "€95.00"
};

## SummaryTable

SummaryTable displays order details in a table, each row has information on name and price for each item.

<Canvas>
  <Story name="Default" args={tableArgs}>
    {(args) => <SummaryTable {...args} />}
  </Story>
</Canvas>

## Props

<ArgsTable of={SummaryTable} />